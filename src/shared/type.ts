export type AnimationType = {
  opacity?: number;
  scale?: number;
  duration?: number;
  x?: string;
  y?: string;
  delay?: number;
};

export type footerLinkItemType = {
  id: number;
  label: string;
  icon?: React.ReactNode;
};
